export interface userContext {
  inputFindValue: string,
  setInputFindValue: (value:string) => void,
}


export interface pageContext {
  numPageValue: number
  setNumPageValue: (value:number) => void,
}
